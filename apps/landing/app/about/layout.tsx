export const metadata = {
  title: 'About — Webfudge Platform',
  description: 'Learn about Webfudge Systems Platform',
}

export default function AboutLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
